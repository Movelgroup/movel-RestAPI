FROM nginx:alpine

# Copy nginx configuration
COPY nginx.conf /etc/nginx/conf.d/default.conf

# Copy the application files
COPY . /usr/share/nginx/html

# Expose port 8080 to match Cloud Run
EXPOSE 8080

CMD ["nginx", "-g", "daemon off;"]
